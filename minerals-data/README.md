# minerals-data

Query and analyze structured CSV datasets on critical minerals production, trade, and supply chains

## Setup

```bash
cd minerals-data
python3 -m venv .venv && source .venv/bin/activate && pip install pandas -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/query_data.py --help
```

## Dependencies

- `pandas`
