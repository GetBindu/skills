# anndata

Data structure for annotated matrices in single-cell analysis.

## Setup

```bash
cd anndata
python3 -m venv .venv && source .venv/bin/activate && pip install anndata -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/demo.py --help
```

## Dependencies

- `anndata`
