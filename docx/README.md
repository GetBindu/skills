# docx

Extract text, tables, headings, and metadata from Microsoft Word .

## Setup

```bash
cd docx
python3 -m venv .venv && source .venv/bin/activate && pip install docx -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/docx_extract.py --help
```

## Dependencies

- `docx`
