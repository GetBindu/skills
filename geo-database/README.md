# geo-database

Access NCBI GEO for gene expression/genomics data.

## Setup

```bash
cd geo-database
python3 -m venv .venv && source .venv/bin/activate && pip install requests -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/query.py --help
```

## Dependencies

- `requests`
