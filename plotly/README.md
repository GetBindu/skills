# plotly

Interactive visualization library.

## Setup

```bash
cd plotly
python3 -m venv .venv && source .venv/bin/activate && pip install plotly -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/demo.py --help
```

## Dependencies

- `plotly`
