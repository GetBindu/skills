# dask

Distributed computing for larger-than-RAM pandas/NumPy workflows.

## Setup

```bash
cd dask
python3 -m venv .venv && source .venv/bin/activate && pip install dask -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/demo.py --help
```

## Dependencies

- `dask`
