# precision-medicine-stratification

ToolUniverse workflow — Precision Medicine Stratification

## Setup

```bash
cd precision-medicine-stratification
python3 -m venv .venv && source .venv/bin/activate && pip install tooluniverse -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/run.py --help
```

## Dependencies

- `tooluniverse`
