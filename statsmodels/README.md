# statsmodels

Statistical models library for Python.

## Setup

```bash
cd statsmodels
python3 -m venv .venv && source .venv/bin/activate && pip install numpy statsmodels -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/demo.py --help
```

## Dependencies

- `numpy`
- `statsmodels`
