# sympy

Use this skill when working with symbolic mathematics in Python.

## Setup

```bash
cd sympy
python3 -m venv .venv && source .venv/bin/activate && pip install sympy -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/demo.py --help
```

## Dependencies

- `sympy`
