# polars

Fast in-memory DataFrame library for datasets that fit in RAM.

## Setup

```bash
cd polars
python3 -m venv .venv && source .venv/bin/activate && pip install polars -q
```

## Environment variables

None.

## Usage

```bash
python3 scripts/demo.py --help
```

## Dependencies

- `polars`
